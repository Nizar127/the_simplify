<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ainshafiqah.thesimplify">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.Simplify">
        <activity android:name=".UpdateStatus"></activity>
        <activity android:name=".CompletedOrderActivity" />
        <activity android:name=".OrderPackActivity" />
        <activity android:name=".OrderShipActivity" />
        <activity android:name=".OrderDeliverActivity" />
        <activity android:name=".StatusActivity" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".EditProfileActivity" />
        <activity android:name=".AddOrder" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".OrderActivity" />
        <activity android:name=".MoreActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegisterActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>